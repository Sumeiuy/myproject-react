/**
 * @Descripter: 排序
 * @Author: K0170179
 * @Date: 2018/7/17
 */

// 升序降序的方向值
const DESC = 'desc'; // 降序
const ASC = 'asc'; // 升序

// 提供一个降序、升序的默认值（通用值，可配置）
const defaultSortDirections = [DESC, ASC];

export default defaultSortDirections;
